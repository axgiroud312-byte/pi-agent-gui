# Pi Agent GUI

为使用 Pi 进行本地编程工作的用户提供桌面工作区。本词汇表统一产品、规格、界面和验收中的概念。

## Language

**工作区（Workspace）**：用户选择的一个本地项目目录，以及围绕该目录组织的会话与改动。工作区可以是 Git 仓库，也可以是普通文件夹。
_Avoid_: 将工作区等同于 Git 分支或执行沙箱。

**会话（Session）**：属于一个工作区、可以保存并继续的对话上下文，包含用户输入、Agent 消息、工具结果及其历史关系。
_Avoid_: 用“任务”同时指代会话和一次运行。

**运行（Run）**：Agent 响应用户输入及其自动续接活动的一段执行周期。运行完全结束时，不再有自动重试、压缩后续接或已排队输入等待继续处理。
_Avoid_: 将“请求已接收”或“单条回复已结束”称为运行完成。

**轮次（Turn）**：一次模型回复及其引发的工具执行。一次运行可以包含多个轮次。
_Avoid_: 将轮次与会话混用。

**转向消息（Steering message）**：在运行中提交、要求 Agent 在当前轮次工具执行完毕后优先调整方向的输入。
_Avoid_: 将转向消息称为立即中断。

**后续消息（Follow-up message）**：排队等待当前 Agent 工作及转向消息处理完毕后再执行的输入。
_Avoid_: 将后续消息描述为立即执行。

**扩展交互（Extension interaction）**：Pi 扩展发起的选择、确认、输入或通知。它表达扩展的实际交互要求，不等同于所有工具都具有逐次审批机制。
_Avoid_: 将任意扩展交互统一称为工具授权。

**工作区改动（Workspace changes）**：工作区当前实际存在的文件变化，其中可能包含用户和其他工具产生的改动。
_Avoid_: 将工作区全部 diff 标记为本会话生成或待执行的修改建议。

**执行环境（Execution target）**：工作区的文件、Agent 和开发进程实际所在的本地系统、WSL 发行版或 SSH 主机。
_Avoid_: 将桌面窗口所在设备默认当成所有命令的执行位置。

**工作树（Worktree）**：同一 Git 仓库中具有独立目录、索引及检出状态的工作副本，用于分离并行任务的文件改动。
_Avoid_: 将工作树等同于会话分支。

**任务（Task）**：有目标、状态和执行记录的一项工作，可以关联一个或多个会话；子任务保留与父任务的关系。
_Avoid_: 将 GitHub Issue 编号、会话 ID 和任务 ID 互换使用。

**能力（Capability）**：用户能够实际使用并验收的一项产品行为。原生支持、桥接支持及经过验证的兼容展示是不同实现路径。
_Avoid_: 将菜单占位、仅显示配置或未验证的第三方支持记为能力已交付。

**原生交互一致性（Native interaction parity）**：同一用户目标下，产品与选定 ZCode 基线具有相同的界面组织、操作入口、操作步骤及可观察的交互反馈，明确登记的产品差异除外。
_Avoid_: 将配色、字号或局部组件相似称为原生交互一致。

**交互基线（Interaction baseline）**：从实际运行的指定版本 ZCode 记录的界面状态、操作路径和反馈，用于比较本产品是否保留了原生体验。
_Avoid_: 将本产品自己的截图或只读源码推断称为原版运行基线。

**兼容差异（Compatibility deviation）**：因产品品牌、已确认的厂商服务排除项或 Pi 的真实能力与语义而产生、具有明确理由和验收记录的界面或行为差异。
_Avoid_: 用兼容差异为未实现功能、无依据的改版或未登记的偏离背书。
