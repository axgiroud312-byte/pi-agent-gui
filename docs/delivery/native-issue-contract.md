本票采用主规格已确认的**原生 ZCode + Pi**路线，参见产品目标、ADR 0001、native-rebase-plan 与 native-ui-parity。保留原生组件、状态、菜单/面板位置和操作路径，在 Agent 服务边界或可复用宿主服务中实现。

- 原生导入和用户界面确认、最小 Pi 适配的阻塞边全部解除后才能领取。方向确认不等于实际界面确认。
- 旧工作树/提交只是候选复用资产；在新原生产品中重新验收前不算本票完成，也不整批合并旧自建前端。
- 本票验收还必须包含原版/产品同状态截图、操作步骤及已登记差异，全部功能行为真实；不能以功能测试或颜色一致代替交互一致。
- 使用原生工程实际提供的检查脚本；继续按原完整 P/I/T 与用户故事验收。必要来源/许可证与迁入代码在同一变更记录。
