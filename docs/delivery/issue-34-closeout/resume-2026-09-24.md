# #34 恢复核验（2026-09-24，进行中）

本轮用户明确授权：只修 PR #39 的既有 CI / Standards / Spec 阻塞，必要本地测试后整理提交、推送，Windows CI 通过后再做独立 Standards / Spec 复审。此前交接中的“不提交推送、只继续旧审查实例”不是本轮停点。本轮未合并、未关闭 #34，不推进后续能力票。

## 已核对

- 唯一工作树：`C:/Users/niilo/AppData/Local/Temp/opencode/pi-native-pi-34`；分支 `issue-34-native-pi-rpc`，远端 HEAD `280e01ed6ca0b0d54bb22e60230b0215e66e3d56`。
- `gh pr view 39`：OPEN / Draft。两个最新 Windows run `35825880290`、`35825878291` 均失败，尚无新提交的 CI 结论。
- 已下载最新失败日志 `D:/Temp/pi34-latest-ci-failed.log`。确认 desktop manifest/lock notice 输入 hash、服务 fixture cwd、AIX optional 安装判断、Pi smoke 等待原版 `/compact` 四类失败。
- 已核对 #34、父规格 #1、阻塞 #33 的 GitHub 正文/评论；#33 CLOSED。`node scripts/check-delivery-plan.mjs --github`：30 票、71 边、27 行一致，唯一实施前沿 #34。
- 保留继承的未提交修复与证据；未跟踪 `%SystemDrive%/` 不读写、不纳入提交。C: 当前约 46 GiB 可用，旧交接 0.5 GiB 为历史状态。

## 本轮实际复跑

以下均在 Windows / Node 24.14.0 / pnpm 10.33.2 执行，不用旧日志替代。

| 检查 | 结果 |
| --- | --- |
| `node scripts/check-native-provenance.mjs` | verified，6,123 项；既有 19 项发行材料差额仍保留 |
| `node scripts/licenses.mjs check` | 通过，1,812 个实装包；不等于严格发行许可审查通过 |
| 新增 17 项 license override 固定来源复核 | 经 GitHub contents API 按固定 commit 取原始字节，与本地文件和 SHA-256 全部一致；不是独立法律裁定 |
| `node --test "scripts/test/*.test.mjs"` | 46/46，零 skip |
| root cwd：`node node_modules/tsx/dist/cli.mjs --test packages/services/test/pi-rpc-client.test.ts` | 31/31 |
| package cwd：`pnpm --dir packages/services exec tsx --test test/pi-rpc-client.test.ts` | 31/31 |
| 新 CI root step：`pnpm exec tsx --test packages/services/test/pi-rpc-client.test.ts packages/desktop/src/host/windowHostControllerProjection.test.ts` | 通过 |
| 定向 scripts oxlint | 26 文件，0 errors / 0 warnings |
| `git diff --check` | 通过（生产整改进行中，提交前须再跑） |

原始日志：`D:/Temp/pi34-resume-{license,scripts-tests,scripts-lint,fixture-root,fixture-package,ci-root-step,host-projection}.log`。

许可来源逐项结果：`D:/Temp/pi34-resume-license-source-verification.json`。Node 直接 fetch 原始 URL 曾遇 ECONNRESET，改用同一固定来源的 GitHub raw contents API 后核验通过，未更换来源或改写通知。

本机继承 PATH 经 pnpm exec 截断曾导致 `node` 找不到；以命令局部 PATH（Node / System32 / Git / npm / bash）重跑通过，未修改用户全局配置。这不是 services fixture 回归失败。

## 最新状态（优先于下方过程记录）

生产首输入约束与冷历史列表标题两个 GUI 回归均已修复。服务 **81/81**，最新日志 `D:/Temp/pi34-title-fix-{services,typecheck,lint}.log`。主会话已重新构建 Host：

- `D:/Temp/pi34-final-pi-gui-5/`：Pi 流式、真实 read、Stop、同 JSONL 重启恢复零重发、滚动、文件标签和双会话操作通过。
- `D:/Temp/pi34-final-native-smoke-7/`：最终脚本/生产源码 18 组动作、48 图通过；保留全部断言、页面错误/残留进程为空。窗口按真实 show 事件等待再进行既有 onboarding 等待；模型 submenu 使用原生键盘 ArrowRight，避免 OS 鼠标移动关闭 hover submenu。harness 不绕过服务或修改产品状态。
- 两个 smoke 失败时展开原生详情以记录实际原因；所有旧失败目录保留。共用窗口 resize helper 解决 400 行 lint 门禁，没有 suppression/上调限额。最终 scripts lint 0 errors/0 warnings、脚本回归46/46，日志 `D:/Temp/pi34-final-{smoke-lint-2,scripts-tests-2}.log`。
- 全仓 typecheck、architecture 通过（`pi34-final-after-title-*`）；该轮全仓 lint 的唯一 error 为上述已修复脚本行数，提交前再次复跑。
- **打包尚阻塞**：`D:/Temp/pi34-final-bundle.log` 的 afterPack asar extract 返回 0xC0000142，electron-builder exit1；残留 exe/app.asar 不是合格包。实施任务正在诊断重试，禁止宣称包验收通过。
- 尚未提交推送、尚无最新绿 Windows CI、尚未启动独立复审；PR Draft / #34 OPEN。

## 集成过程记录（保留失败依据）

- 子任务报告 S1–S8 修复、服务包 79/79、services/UI 类型及定向 lint 通过；主会话不将任务报告直接当作最终验收。
- 主会话重新准备 runtime assets、metadata，并顺序执行 production tsup / Vite，全部 exit 0；日志 `D:/Temp/pi34-final-{runtime-assets,build-metadata,tsup,vite}.log`。
- 新构建 GUI 失败：`D:/Temp/pi34-final-native-smoke-2/` 通过原生外观四组合、Lexical、slash、mention 后，普通首输入显示 SEND_FAILED，模型请求为空。补充 `D:/Temp/pi34-final-pi-gui-2/pi-native-gui-report.json` 通过原生“展开详情”得到 **pi.commandNotImplemented**。已交回实施任务修正新增约束与真实原生 payload 的兼容，必须补回归并再次重建，不能凭 79 项通过忽略。
- 首轮 native smoke 因窗口隐藏，`getAllWindows().find(isVisible)` 无结果失败；harness 改为绑定 Playwright 当前 page 的 BrowserWindow，再 restore/show/resize，第二轮矩阵通过。没有改产品窗口行为或放松布局断言。
- 两个 smoke 的失败采集现会点击原生详情按钮记录真实错误，保留失败日志，不绕过失败。
- 活跃 bash 关闭曾红；主会话独立 probe 证明普通 owned Node/bash 树 taskkill 可用，真实 Pi 在 abort_bash ACK 后等到原请求 cancelled:true 会清理子进程。实施任务据此加入握手等待并报告直接/Agent bash 两项绿色；生产兜底异常路径仍待独立复核。

## 当前修改与剩余

- CI 增补 root cwd transport 回归及 native Host projection，保留原 package cwd 服务全测试、全部失败门禁和独立产品 GUI smoke。
- 单一 worker 独占 Pi 服务/服务测试，处理联合记录 S1–S8 的剩余生产阻塞；主会话负责 CI、许可、冒烟脚本及最后集成验收。没有启动独立复审，也没有创建可复用角色。
- 当前服务和开发构建 GUI 结果见最新状态；剩余打包验证、提交前检查、提交推送/Windows CI及独立复审仍是关卡，不能由局部通过替代。
- 独立 Standards / Spec（含许可来源裁定与视觉缺口）必须在 CI 通过后重新进行；失败时继续保持 PR Draft / #34 OPEN。
