# 产品目标：ZCode 原生工作台，Pi Agent 内核

确认日期：2026-09-22。来源：用户两轮 grill-with-docs 决策；权威规格为 [Issue #1](https://github.com/axgiroud312-byte/pi-agent-gui/issues/1) 的最新正文与评论。

## 产品定义

以 **ZCode 原生开源工程为产品底座**，保留它的界面组织和用户交互，在 Agent 服务边界接入 **Pi Coding Agent**。用户看到并操作的是 ZCode 原生工作台的 Pi 版本：产品品牌替换，获准排除的厂商入口移除，其余默认保留原生体验。

原生交互一致性包括窗口与面板布局、工作区/任务/会话组织、标签/拖拽/分栏、Lexical 输入框、附件/引用/命令、消息与工具时间线、队列、菜单、设置、快捷键、焦点、滚动及状态反馈。仅复用颜色、字号、圆角或独立组件不满足目标。

## 已确认的边界

| 决策 | 确认结果 |
| --- | --- |
| 工程起点 | 导入固定版本 ZCode 原生工程及必要的依赖包/资源，保留 Electron 壳、原生 UI、状态管理、服务接口和可复用本地宿主服务。允许替换当前自建工程结构。 |
| 界面依据 | `zai-org/ZCode@872ad960de7ec172591f7e1952f7849229f94521`。先实际运行并记录交互基线；实施时核对提交，不随 main 漂移。 |
| Agent | 最终产品全部 Agent 执行由 `@earendil-works/pi-coding-agent@0.87.0` 的 RPC、公开扩展和版本化兼容层承接；Pi 拥有模型循环、工具、会话历史、队列、重试及压缩语义。 |
| 首版范围 | 原 84 条用户故事、P01–P34、I01–I18、T01–T20 保留。Pi 原生未内建的 Plan、子 Agent、MCP、调度等仍通过扩展/宿主补齐。确实无法实现的项目逐项提交用户决定，保持未完成。 |
| 交付形态 | Windows 桌面；包含原定 WSL/SSH 和 Pi CLI 历史互通。独立 Web 产品、ZCode 风格独立 CLI 发行不纳入首版验收；可保留构建/宿主需要的共享源码。 |
| 特有入口 | Pi 会话树、双队列、扩展 TUI 等优先放入原生菜单、输入区、设置、详情或 Side Pane；必要新增最小入口并登记差异。 |
| 旧成果 | 旧主线和所有功能分支保留。Pi RPC、桥、宿主逻辑与测试按模块审查迁入；旧自建前端和 #32 的样式移植不得整批作为新界面底座。 |
| 用户关卡 | 原生底座跑通并完成品牌/厂商入口处理后，提交原版与产品的对应截图和操作记录，**暂停等待用户明确确认**。确认前不开展 Pi 替换或继续旧功能批次。确认后按依赖自动实施。 |

## 原生工程与适配边界

原生 ZCode 前端不仅是 `packages/ui` 的样式层；它依赖工作台/会话状态、平台能力、服务访问器、会话订阅和命令确认协议。保留这些用户交互结构，在服务边界将原有命令/订阅映射到 Pi，而不是改完模型 provider 就宣称更换了 Agent。

原 ZCode Agent 仅可在隔离的原版对照阶段临时运行，用于建立真实交互记录。最终 Pi 产品的执行路径不得回退到原 Agent；前端继续需要的协议/类型/投影代码可保留，原引擎执行依赖应解除。

沿用上游锁定的构建体系建立可运行基线，再记录必要兼容调整。旧原型的 Electron/React/组件版本、npm 脚本和 assistant-ui 选择不约束新原生工程。Pi 版本与真实协议合同继续固定；IDE 领域仍区分工作区、会话、任务、运行和执行环境。

## 已允许的产品差异

1. 替换 ZCode 名称、Logo、产品标识和厂商推广文案；保留法律归属/许可证/NOTICE 中应保留的名称。
2. 移除 Z.AI/BigModel 产品账户、团队/充值/Coding Plan/专属配额、闲时票据等厂商商业入口。**Pi 本身支持的 provider/API-key/OAuth 登录仍保留**，使用原生设置结构呈现。
3. 厂商云分享/反馈/更新等服务不能直接作为本产品服务使用；原规格要求的分享、诊断、更新通过本项目宿主和可用服务实现，尽量保留原入口位置。
4. 官方厂商 MCP/包分发账号入口可移除；通用 MCP、Pi 包/资源能力仍必需。普通文件、Git、终端、预览、WSL/SSH 等本地/远端宿主功能仍必需。
5. 公开 ZCode 中不可用的 Computer Use 占位不作为已交付能力，也不新增为首版要求。
6. Pi 真实语义与 ZCode 原语义存在冲突时，保持 Pi 事实，在原生交互结构内做最小调整，记录前后行为与原因。不得显示并不存在的审批、运行完成或工具能力。

具体入口、截图与处理结果记录在 [原生一致性验收](delivery/native-ui-parity.md)；每项排除都必须查明来源，不能把实现困难归为“Pi 不支持”。

## 完成条件

- 原生界面用户关卡有明确确认记录。
- 全部矩阵与用户故事在**新原生工作台 → 服务适配 → Pi RPC/宿主**链路中通过验收。
- 同分辨率、主题和状态下，与原版的界面和操作路径逐项对照；未登记的偏离作为缺陷处理。
- 全部功能 PR 经 Standards/Spec 审查及必要 CI；真实 Pi、Windows 安装/升级/恢复与相关外部环境完成。
- 最终文档、来源许可、兼容版本和安装包对应实际代码；只有全部满足后关闭 #1。

开发顺序见 [重建计划](delivery/native-rebase-plan.md)。新上下文执行入口见 [next-context.md](delivery/next-context.md)。
