# Issue 驱动开发

## 从规格到实施

1. 在 GitHub Issue 中记录用户目标、方案、用户故事、实现决定、测试决定和范围。
2. `to-spec` 生成的完整规格使用 `ready-for-agent`。大型规格先交给 `to-tickets` 拆分，标签不表示所有工作必须由一次会话完成。
3. 实施 Issue 写明父规格、用户可见结果、验收条件和阻塞关系。按纵向切片拆分，让每个 Issue 都能展示一个可运行结果。
   分期是实现顺序，完整首版的能力范围以主规格矩阵为准；某一阶段完成不代表首版完成。
4. 优先使用 GitHub 原生 issue dependencies。不可用时使用 `Blocked by: #N`，并在领取前核实所有阻塞项均已关闭。
5. 开始实施前读取正文与评论；将领取状态记录到 Issue，再使用 `issue-<number>-<short-name>` 分支。

## 提交与审查

- 提交聚焦于对应 Issue，沿用 `docs:`、`feat:`、`fix:` 等简短前缀。
- PR 写明 `Closes #<实施 Issue>`，以及 `Refs #<父规格>`。部分功能完成时保持父规格开启。
- 验收报告列出实际执行的命令及结果；有界面变化时附关键状态截图；有 RPC 行为变化时附对应场景结果。
- 组件测试服务于可观察行为。主要验收链路是 GUI → 桌面宿主 → RPC 子进程；真实模型冒烟和可重复的离线测试分别报告。
- 复用源代码时，在同一变更中记录上游 URL、固定提交或版本、许可证和必要 notices；仅参考交互时标明参考性质。

## 完成标准

对应 Issue 的验收条件通过，必要检查完成，审查与实际结果一致后再关闭。父规格中每项必需能力都有交付与验收证据后才达到首版完成标准。若调整规格，在 GitHub Issue 留下原因与新的验收条件，避免仅在聊天中改变范围。

当前仓库处于文档初始化阶段。第一张实施票需要建立真实的开发、类型检查、测试、构建和 Windows 打包命令，之后以这些脚本为准。
